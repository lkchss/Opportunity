# Opportunity Finder — install.
#   pip install -r requirements.txt
#
# Installs the core + the web app. Add ONE model backend below (or none).

# --- Core (always needed) ---
python-dotenv>=1.0.0       # reads .env
pypdf>=4.0.0               # parse an uploaded résumé / context PDF

# --- Web app (python -m finder.server) ---
flask>=3.0.0
gunicorn>=21.2.0           # production server (deploy); dev uses Flask's built-in

# --- Optional Streamlit UI (streamlit run finder/app.py) ---
# An alternative to the Flask web app; not required by the CLI or the web app.
# streamlit>=1.32.0

# --- Pick a model backend (REQUIRED — the model is the engine) ---
# Claude API (uses Claude's native web search for real, current listings):
#   pip install "anthropic>=0.40.0"
#
# OpenAI, or any OpenAI-compatible server — covers OpenAI, OpenRouter, Groq,
# Together, Ollama (http://localhost:11434/v1), LM Studio, vLLM, llama.cpp:
#   pip install "openai>=1.0.0"
